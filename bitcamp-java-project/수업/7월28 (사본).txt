jvm 동작
int j;
int i;
j = i++;
임시 메모리에 i 값을 넣고 i를 증가시키고
j를 임시메모리에 저장한 i 값을 저장한다
    //int j;
    //int = temp;
    //temp = i;
    //i = i + 1;
    //j = temp;

// 대괄호가 없을때 else는 가장 가까운 if 문과 연결되어 묶인다
// 자바 설정 자동으로 줄바꾸고 하는거 프리퍼런스 자바 에디터
